{
  "name": "discoveryintel",
  "version": "1.0.0",
  "description": "AI Litigation Intelligence Platform",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:shared && npm run build:backend && npm run build:frontend",
    "build:win": "npm run build:shared && npm run build:backend && npm run build:frontend:win",
    "build:shared": "cd shared && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build:frontend:win": "powershell -ExecutionPolicy Bypass -File scripts/build-frontend-windows.ps1",
    "clean": "node scripts/clean.js",
    "install:all": "npm install && cd shared && npm install && npm run build && cd ../backend && npm install && cd ../frontend && npm install",
    "reinstall": "npm run clean && npm run install:all"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
